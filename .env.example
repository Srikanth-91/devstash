# Neon PostgreSQL connection string
# Use the "direct connection" string from your Neon console (not the pooled one)
# for Prisma migrations. For the runtime adapter you can use either.
DATABASE_URL="postgresql://user:password@ep-xxxx.us-east-1.aws.neon.tech/dbname?sslmode=require"
